{
  "name": "express-export-test",
  "version": "1.0.0",
  "description": "Test exporting 500k rows using Go-powered export engine with Express",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:go": "cd ../.. && make build",
    "build:node": "cd ../../node-wrapper && npm install && npm run build",
    "setup": "npm run build:go && npm run build:node && npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
